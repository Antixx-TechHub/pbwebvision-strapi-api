'use strict';

/**
 * challengesandtrack router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::challengesandtrack.challengesandtrack');
